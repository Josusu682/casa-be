<template>
  <section class="relative w-full h-screen min-h-[600px] overflow-hidden">
    
    <!-- Capa 1: Foto principal (fondo) -->
    <img
      alt="Mujer mirando al cielo - Casa BE"
      class="absolute inset-0 w-full h-full object-cover z-0"
      src="/images/fondo.png" 
    />

    <!-- Capa 2: Botón CTA (por encima de la foto) -->
    <div class="absolute inset-0 z-10 flex items-center justify-center px-6">
     
    </div>

    <!-- Capa 3: Overlay fondo.png (por encima del botón, sin bloquear clicks) -->
   

  </section>
</template>